/**
 * look.js — Head-turning mechanic (Disabled)
 */

const Look = (() => {
  function setLocked(val) {}
  function isLocked() { return false; }
  function init() {}
  return { init, setLocked, isLocked };
})();
